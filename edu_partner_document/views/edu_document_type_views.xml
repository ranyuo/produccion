<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View edu.document.type View List -->
    <record id="view_edu_document_type_list" model="ir.ui.view">
        <field name="name">view.edu.document.type.list</field>
        <field name="model">edu.document.type</field>
        <field name="arch" type="xml">
            <list string="Tipos de documento" editable="bottom">
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- Action edu.document.type -->
    <record id="action_edu_document_type" model="ir.actions.act_window">
        <field name="name">Tipos de documento</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">edu.document.type</field>
        <field name="view_mode">list</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Document Type.
            </p>
        </field>
    </record>

</odoo>
